<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- دسترسی‌های اینترنت و شبکه -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- دسترسی‌های نوتیفیکیشن و زمان‌بندی آلارم -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- برای نمایش صفحه‌ی تمام‌صفحه‌ی یادآوری روی صفحه‌ی قفل / وقتی گوشی خاموشه، دقیقاً
         مثل اپ‌های آلارم و تماس -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- درخواست معافیت از بهینه‌سازی باتری؛ بدون این اندروید و OEM ها (شیائومی/هواوی/سامسونگ)
         می‌تونن آلارم‌های دقیق رو در طول زمان محدود کنن حتی اگه exact-alarm مجاز باشه -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- دسترسی ذخیره فایل بک‌آآپ در حافظه -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MyPlanner">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- دریافت‌کننده نوتیفیکیشن -->
        <receiver android:name=".ReminderReceiver" android:exported="false" />

        <!-- صفحه‌ی تمام‌صفحه‌ی یادآوری: وقتی گوشی قفله یا صفحه خاموشه، همین صفحه با روشن
             کردن خودکار صفحه باز میشه (بدون باز کردن قفل امنیتی گوشی) -->
        <activity
            android:name=".ReminderFullScreenActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" />

        <!-- دریافت‌کننده دکمه‌های «تمام» و «۱۵ دقیقه دیگه» روی نوتیف‌ها، و کشیدن/ردکردن نوتیف -->
        <receiver android:name=".NotificationActionReceiver" android:exported="false" />

        <!-- دریافت‌کننده روشن شدن گوشی + آپدیت/نصب مجدد اپ؛ هردو باعث پاک شدن آلارم‌های قبلی میشن
             و باید یادآوری‌ها از نو (و با جبران عقب‌افتادگی) تنظیم بشن -->
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>